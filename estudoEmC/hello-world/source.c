#include <stdio.h> //#include é um pré processador que diz ao programa para incluir bibliotecas especificadas, nesse caso <stdio.h>
//a biblioteca stdio contém funções para a exibição e entrada de dados como printf() e scanf().

int main(){ // int main é a função principal de inicio do programa.
    // printf é o comando para exibir uma mensagem ao usuário.
    printf("Hello world");
    return 0; // O return 0 indica o "exit status" do programa, em outras palavras o programa encerra com essa declaração
}